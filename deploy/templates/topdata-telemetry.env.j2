# Rendered by the topdata-telemetry deploy playbook from ansible vault vars.
TOPDATA_TELEMETRY_SHOPS_ROOT={{ shops_root }}
TOPDATA_TELEMETRY_AUTH_USERNAME={{ auth_username }}
TOPDATA_TELEMETRY_AUTH_PASSWORD={{ auth_password }}
TOPDATA_TELEMETRY_LISTEN_ADDRESS={{ listen_address }}

# Safe defaults for slow storage (SD/HDD-class fleet). The agent's disk walk is
# throttled and deferred on restart so it can never saturate the host's disk.
# IOSchedulingClass=idle in the systemd unit is the backstop that makes these safe.
TOPDATA_TELEMETRY_DISK_SCAN_DEFER_ON_STATE=true
TOPDATA_TELEMETRY_DISK_STATE_SAVE_INTERVAL=30s
TOPDATA_TELEMETRY_DISK_SCAN_YIELD_EVERY=100
TOPDATA_TELEMETRY_DISK_SCAN_YIELD_SLEEP=2ms
